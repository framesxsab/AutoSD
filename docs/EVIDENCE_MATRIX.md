# Evidence Matrix — Capability → Evidence State

Every capability fits exactly one current status. Automated evidence is maintainer-generated (`npm test`, `npm run evaluate`); other columns name what is still required.

| Capability                                       | Implementation                                                  | Automated evidence                                                                         | External evidence required                           | Hardware evidence required                                                                          | User validation required | Current status                                                                 |
| ------------------------------------------------ | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------ |
| Document ingestion (chunking, hashing)           | `src/retrieval/chunker.ts`, `SnapshotIndex`                     | `T02-INGEST` PASS, `tests/retrieval/chunker.test.ts`                                       | Real corpus with hand-judged relevance (for quality) | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| Hash-diffed snapshot / incremental indexing      | `SnapshotIndex.index()`                                         | `T11` subchecks `changed-version`, `stale-snapshot` + `tests/retrieval/snapshot.test.ts`   | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| Hybrid retrieval (BM25 + vectors, RRF k=60)      | `src/retrieval/pipeline.ts` + `reranker.ts`                     | `T03-RETRIEVAL`, `T11` 9 subchecks (similar, distractor, overlap, contradictory, citation) | Real frozen corpus + recall@k judgments (GFI-3)      | —                                                                                                   | —                        | **SOFTWARE-VERIFIED** (quality on real corpus: `EXTERNAL EVALUATION REQUIRED`) |
| Citation correctness                             | `ResearchWorkflow.run()`                                        | `T04-CITATIONS` + `T11` `citation-correctness`                                             | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| Reader pagination                                | `ReaderWorkflow.paginate()`                                     | `T05-READER` + `tests/workflows/workflows.test.ts`                                         | —                                                    | —                                                                                                   | Reading comfort study    | **SOFTWARE-VERIFIED** (`USER-VALIDATION-PENDING` for comfort)                  |
| Session persistence + export                     | `ResearchWorkflow.saveToDisk/loadFromDisk`, `exportLastSession` | `T06-SESSION-PERSIST`, `T07-EXPORT` + persistence tests                                    | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| Deleted / replaced document handling             | `SnapshotIndex` removed detection                               | `T11` `deleted-replaced`                                                                   | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| VirtualDevice tactile pipeline                   | `VirtualDevice` + `textToDots`                                  | `T08-TACTILE-VIRTUAL`                                                                      | —                                                    | —                                                                                                   | Readability of dots      | **SOFTWARE-VERIFIED** (`USER-VALIDATION-PENDING` for readability)              |
| MockDevice fixture                               | `MockDevice`                                                    | `tests/devices/devices.test.ts`                                                            | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| HIDDevice adapter                                | `HIDDevice` dynamic import + fallback                           | Fallback contract tests only                                                               | —                                                    | **HARDWARE REQUIRED**: first `HARDWARE-CONNECTED` report per `docs/reports/hardware-integration.md` | —                        | **SOFTWARE-SCAFFOLDED** (`HARDWARE-DEPENDENT` in matrix row 4)                 |
| DeviceManager / Registry / hot-swap              | `DeviceManager`, `Registry`                                     | `T10-RECOVERY` + `tests/core/registry.test.ts`                                             | —                                                    | Hot-swap with real handle (in hardware report)                                                      | —                        | **SOFTWARE-VERIFIED** (hot-swap with hardware: `HARDWARE REQUIRED`)            |
| Diagnostics + privacy scan                       | `collectDiagnostics`, `sanitize`, `scanEvidence`                | `T09-DIAGNOSTICS`                                                                          | External report intake via `--validate`              | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |
| Browser app (9 routes, onboarding, a11y helpers) | `src/app/*`, `src/ui/*`                                         | `T01-STARTUP` (e2e smoke) + `tests/ui/*` + Lighthouse                                      | Manual browser walkthrough `MANUAL`                  | —                                                                                                   | Screen-reader study      | **SOFTWARE-VERIFIED** (`USER-VALIDATION-PENDING` for AT)                       |
| Plugin system                                    | `PluginHost`, `PluginRegistry`                                  | `tests/examples/minimal-tactile-plugin.test.ts`                                            | —                                                    | —                                                                                                   | —                        | **SOFTWARE-VERIFIED**                                                          |

**How to read the matrix:** `Current status` is the strongest `validationLevel` that has evidence today. Moving a row requires the evidence named in its “required” column and a reviewed report — see `docs/BASELINE_MIGRATION.md` and `docs/CAPABILITY_MATRIX.md` “What would move the status.”
